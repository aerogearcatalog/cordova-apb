version: 1.0
name: cordova-app-apb
description: Sets up a Cordova App representation.
bindable: False
async: optional
tags: 
  - mobile
metadata:
  displayName: Cordova App
  console.openshift.io/iconClass: icon icon-cordova
plans:
  - name: default
    description: Sets up a Cordova App representation.
    free: True
    metadata: {}
    parameters:
    - name: appIdentifier
      required: True
      type: string
      title: Package Name     
    - name: appName
      required: True
      default: myapp
      type: string
      title: your new apps name
